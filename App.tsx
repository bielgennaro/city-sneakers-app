import React from 'react';
import { StyleSheet, Text, View, SafeAreaView, TextInput } from 'react-native';
import LoginFields from './src/shared/components/loginFields';

export default function App() {
  return (
    <SafeAreaView>
      <LoginFields />
    </SafeAreaView>

  );
}
